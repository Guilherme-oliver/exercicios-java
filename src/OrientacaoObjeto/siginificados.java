package OrientacaoObjeto;

public class siginificados {
    //public double a;
    //public double b;
    //public double c;

    //public double area() {
       // double p = (a + b + c) / 2.0;
       // return Math.sqrt(p * (p - a) * (p - b) * (p - c));
    //}

    // package OrientacaoObjeto; -> significa pacote da classe
    // public class siginificados -> nome da classe
    // public -> O prefixo "public" indica que o atributo ou método pode ser usado em outros arquivos
    // double a; -> atributos da classe
    //double p = (a + b + c) / 2.0; return Math.sqrt(p * (p - a) * (p - b) * (p - c)); -> corpo do método
    // double -> Tipo do dado que o método retorna (se o método não retorna nada, usa-se a palavra "void")
    // area -> Nome do método
    // () -> Lista de parâmetros do método


    /* Contrutor
    +É uma operação especial da classe, que executa no momento da instanciação do objeto
    +Iso comuns: iniciar valores dos atributos, permitir ou abrigar que o objeto receba dados/ dependências no momento da sua instanciação (injeção de dependência)
    +Se um construtor customizado não for especificado, a classe disponibiliza o construtor padão: Product p = new Product();
    +É possível especificar mais de um construtor na mesma classe (sobrecarga)
     */






}
